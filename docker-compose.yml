version: '3.8'

services:
  zenta-db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: zenta_user
      POSTGRES_PASSWORD: zenta_password
      POSTGRES_DB: zenta_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
